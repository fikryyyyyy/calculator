modules / ScientificCalculator.js;
class ScientificCalculator {
  initialize() {
    // Initialize any necessary variables or settings for the scientific calculator
  }

  // Add other methods for scientific calculator operations as needed
  square(a) {
    return a ** 2;
  }

  squareRoot(a) {
    return Math.sqrt(a);
  }

  pi() {
    return Math.PI;
  }

  e() {
    return Math.E;
  }
}

export default ScientificCalculator;
